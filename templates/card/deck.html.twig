{% extends "base.html.twig" %}

{% block title %} Deck {% endblock %}


{% block content %} 


<p>Deck content:</p>

<div class = "deck">

    <div class = "cards">
        {% for card in get_cards[0:13] %}
            <div class= "card">
                <div class = "{{ card.getDetails[1] }}">
                    {{ card.getDetails[1] }}
                </div>
                <div>
                    {{ card.getDetails[0] }}
                </div>
            </div>
        {% endfor %}
    </div>

    <div class = "cards">
        {% for card in get_cards[13:13] %}
            <div class= "card">
                <div class = "{{ card.getDetails[1] }}">
                    {{ card.getDetails[1] }}
                </div>
                <div>
                    {{ card.getDetails[0] }}
                </div>
            </div>
        {% endfor %}
    </div>

    <div class = "cards">
        {% for card in get_cards[26:13] %}
            <div class= "card">
                <div class = "{{ card.getDetails[1] }}">
                    {{ card.getDetails[1] }}
                </div>
                <div>
                    {{ card.getDetails[0] }}
                </div>
            </div>
        {% endfor %}
    </div>

    <div class = "cards">
        {% for card in get_cards[39:13] %}
            <div class= "card">
                <div class = "{{ card.getDetails[1] }}">
                    {{ card.getDetails[1] }}
                </div>
                <div>
                    {{ card.getDetails[0] }}
                </div>
            </div>
        {% endfor %}
    </div>
    </div>



{% endblock %}
